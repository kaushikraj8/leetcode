class Solution {
public:
    bool canPartition(vector<int>& nums) {
     std::vector<int> arr1;
     std::vector<int> arr2;
     int i=0;
     int n=nums.size()-1;
     sort(nums.begin(),nums.end());
     while(i<n) {
     	if()
     }

        
    }
};